---
phase: 02-core-chat-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - routes/chat.py
  - templates/chat_widget.html
  - tests/test_chat_routes.py
autonomous: false
requirements:
  - CHAT-06
  - CHAT-07

must_haves:
  truths:
    - "User can open a conversation list panel inside the chat widget"
    - "Conversation list shows previous sessions with titles and timestamps"
    - "Clicking a conversation loads it and displays prior messages in the widget"
    - "User A cannot see User B's conversations in the list"
    - "Clicking 'New' starts a fresh conversation with no prior context"
  artifacts:
    - path: "routes/chat.py"
      provides: "GET /api/chat/conversations endpoint and GET /api/chat/conversations/<id>/messages endpoint"
      contains: "list_conversations"
    - path: "templates/chat_widget.html"
      provides: "Conversation list panel wired to endpoint, loadConversation fetches and renders history"
      contains: "showConversationList"
    - path: "tests/test_chat_routes.py"
      provides: "Tests for conversation list and messages endpoints"
      min_lines: 20
  key_links:
    - from: "templates/chat_widget.html"
      to: "/api/chat/conversations"
      via: "fetch GET in showConversationList()"
      pattern: "fetch.*api/chat/conversations"
    - from: "templates/chat_widget.html"
      to: "/api/chat/conversations/{id}/messages"
      via: "fetch GET in loadConversation()"
      pattern: "fetch.*api/chat/conversations.*messages"
    - from: "routes/chat.py"
      to: "models.py"
      via: "get_conversations_for_user(user_id) and get_recent_messages(conversation_id)"
      pattern: "get_conversations_for_user|get_recent_messages"
---

<objective>
Add the conversation list API endpoint, a conversation messages endpoint, and wire the conversation list panel in the widget so users can view, continue (with visible history), and create conversations.

Purpose: Enables session management — users can return to previous conversations, see their prior messages, and start new ones, completing the multi-session chat experience.
Output: `GET /api/chat/conversations` endpoint, `GET /api/chat/conversations/<id>/messages` endpoint, wired conversation list panel in widget, integration tests
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-chat-experience/02-RESEARCH.md
@.planning/phases/02-core-chat-experience/02-01-SUMMARY.md

@routes/chat.py
@templates/chat_widget.html
@models.py
@tests/test_chat_routes.py

<interfaces>
<!-- Existing model function -->
From models.py:
  get_conversations_for_user(user_id, limit=20) -> list[dict]
    Returns: rows with keys 'id' (uuid), 'title' (str|None), 'last_active_at' (datetime)
    Ordered by: last_active_at DESC

  get_recent_messages(conversation_id, limit=16) -> list[dict]
    Returns: rows with keys 'role', 'content', ordered oldest-first

  get_conversation(conversation_id, user_id) -> dict|None
    Returns: conversation row if owned by user_id, None otherwise (cross-user isolation)

<!-- Widget JS functions already stubbed in Plan 01 -->
showConversationList() — shows conv-list-panel, fetches /api/chat/conversations
hideConversationList() — hides conv-list-panel
loadConversation(convId) — sets currentConversationId, clears messages
newConversation() — clears currentConversationId and messages

<!-- Auth decorator -->
From auth.py:
  @api_login_required — returns 401 JSON if not authenticated
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conversation list and messages endpoints, wire conversation history display in widget</name>
  <files>routes/chat.py, templates/chat_widget.html, tests/test_chat_routes.py</files>
  <behavior>
    - Test: GET /api/chat/conversations returns 200 with JSON containing 'conversations' array
    - Test: Each conversation object has 'id', 'title', 'last_active_at' keys
    - Test: Conversations with null title display as 'Untitled conversation'
    - Test: Unauthenticated GET /api/chat/conversations returns 401
    - Test: User A's request only returns User A's conversations (SEC-10 via get_conversations_for_user)
    - Test: GET /api/chat/conversations/{id}/messages returns 200 with 'messages' array containing 'role' and 'content' keys
    - Test: GET /api/chat/conversations/{id}/messages returns 404 for conversation not owned by user
    - Test: Unauthenticated GET /api/chat/conversations/{id}/messages returns 401
  </behavior>
  <action>
Three parts: conversation list endpoint, messages endpoint, and widget wiring.

**1. Conversation List Endpoint (`routes/chat.py`):**

Add a new route to the `chat_bp` blueprint:

```python
@chat_bp.route('/api/chat/conversations', methods=['GET'])
@api_login_required
def list_conversations():
    user_id = session.get('user_id')
    convs = models.get_conversations_for_user(user_id, limit=20)
    return {
        'conversations': [
            {
                'id': str(c['id']),
                'title': c.get('title') or 'Untitled conversation',
                'last_active_at': str(c['last_active_at'])[:16].replace('T', ' '),
            }
            for c in convs
        ]
    }
```

**2. Conversation Messages Endpoint (`routes/chat.py`):**

Add a second endpoint to fetch messages for a specific conversation. This enables `loadConversation()` to display prior messages visually in the widget (ROADMAP success criterion 4: "prior context loaded").

```python
@chat_bp.route('/api/chat/conversations/<conversation_id>/messages', methods=['GET'])
@api_login_required
def get_conversation_messages(conversation_id):
    user_id = session.get('user_id')
    conv = models.get_conversation(conversation_id, user_id)
    if conv is None:
        return {'error': 'Conversation not found'}, 404
    messages = models.get_recent_messages(conversation_id, limit=16)
    return {
        'messages': [
            {'role': m['role'], 'content': m['content']}
            for m in messages
        ]
    }
```

The `get_conversation(conversation_id, user_id)` call ensures cross-user isolation — returns None if the conversation doesn't belong to the authenticated user.

**3. Widget Wiring (`templates/chat_widget.html`):**

The Plan 01 widget already has `showConversationList()`, `hideConversationList()`, and `loadConversation()` stubbed. Update them to be fully functional:

- `showConversationList()`: Should already fetch from `/api/chat/conversations` and render the list. Verify it handles empty conversations list ("No conversations yet."), loading state, and fetch errors. Each conversation item should be clickable and show title + last_active_at.

- `loadConversation(convId)`: Update to fetch and display prior messages. Replace the stub implementation with:
  1. Set `currentConversationId = convId`
  2. Hide conversation list panel
  3. Clear messages div
  4. Show a loading indicator ("Loading conversation...")
  5. Fetch `GET /api/chat/conversations/${convId}/messages`
  6. For each message in the response, call `appendMessage(msg.role, msg.content)` to render the prior user and assistant messages visually in the widget
  7. If fetch fails, show system note "Failed to load conversation history."
  8. User can then type a new message to continue the thread (server-side also loads history via `get_recent_messages` in `process_message`)

  This ensures the user sees their prior messages when loading a conversation, not a blank chat area.

- `newConversation()`: Verify it clears `currentConversationId = null`, clears messages div, shows system note "New conversation started." (CHAT-07). This is already stubbed in Plan 01.

**4. Tests (`tests/test_chat_routes.py`):**

Add test functions:
- `test_list_conversations` — authenticated GET returns 200 with conversations array
- `test_list_conversations_unauthenticated` — GET without auth returns 401
- `test_list_conversations_empty` — new user with no conversations returns empty array
- `test_list_conversations_title_fallback` — null title becomes 'Untitled conversation'
- `test_get_conversation_messages` — authenticated GET with valid conversation returns messages array
- `test_get_conversation_messages_wrong_user` — GET for another user's conversation returns 404
- `test_get_conversation_messages_unauthenticated` — GET without auth returns 401

Use existing test fixtures from `conftest.py` for authentication mocking.
  </action>
  <verify>
    <automated>pytest tests/test_chat_routes.py::test_list_conversations tests/test_chat_routes.py::test_list_conversations_unauthenticated tests/test_chat_routes.py::test_get_conversation_messages tests/test_chat_routes.py::test_get_conversation_messages_wrong_user -x -q</automated>
  </verify>
  <done>
    - GET /api/chat/conversations returns user's conversations with id, title, last_active_at
    - GET /api/chat/conversations/{id}/messages returns prior messages with role and content
    - Messages endpoint returns 404 for conversations not owned by the user
    - Null titles display as 'Untitled conversation'
    - Endpoints require authentication (401 without session)
    - Only the authenticated user's conversations are returned
    - Widget loadConversation() fetches and displays prior messages visually
    - Widget conversation list panel fully functional (show, hide, load with history, new)
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete chat experience end-to-end</name>
  <files>templates/chat_widget.html, routes/chat.py</files>
  <action>
Human verifies the full Phase 2 chat experience: widget with personalized context, conversation list, conversation continuation with visible history, and new conversation creation. This is the final Phase 2 acceptance gate.
  </action>
  <what-built>Complete chat experience: widget + personalized context + conversation management with history display</what-built>
  <how-to-verify>
    1. Start the dev server and log in
    2. Open chat widget, send a message about your training — verify response references actual data (if Strava connected) or gives general cycling advice (if not)
    3. Click "History" button — verify conversation list panel slides in showing your conversations with titles and timestamps
    4. Click a previous conversation — verify prior messages (both user and assistant) appear in the chat area, not a blank panel
    5. Send a follow-up message in the loaded conversation — verify it continues the thread with context
    6. Click "New" — verify it starts a fresh conversation with no prior messages
    7. Navigate to a different page — verify widget state persists
    8. Log out — verify widget disappears
    9. (If possible) Test with a second user account — verify you cannot see the first user's conversations
  </how-to-verify>
  <verify>Manual end-to-end verification of complete chat experience</verify>
  <done>Full chat experience verified: widget, personalization, conversation list with history replay, new conversation, cross-user isolation</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/test_chat_routes.py -x -q` — conversation list and messages endpoint tests pass
- `pytest tests/ -x -q` — full suite green
- Manual: conversation list shows previous sessions
- Manual: loading a conversation displays prior messages visually
- Manual: new conversation creates a fresh thread
</verification>

<success_criteria>
- GET /api/chat/conversations returns properly formatted conversation list
- GET /api/chat/conversations/{id}/messages returns prior messages for the conversation
- Loading a conversation displays prior messages in the widget (not blank)
- Conversation list panel works inside the widget (show, load with history, new)
- Cross-user isolation maintained (user only sees own conversations, 404 for others)
- New conversation creation works via widget button
- Full end-to-end flow verified manually: send message, view history, load conversation with messages, continue conversation, start new
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-chat-experience/02-03-SUMMARY.md`
</output>
