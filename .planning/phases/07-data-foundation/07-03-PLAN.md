---
phase: 07-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - scripts/seed_coaching_profiles.py
  - tests/test_coaching_models.py
autonomous: true
requirements:
  - PROF-02
  - PROF-04

must_haves:
  truths:
    - "Shriram has a coach personality profile matching the persona in CHAT_SYSTEM_PROMPT"
    - "Venki has a coach personality profile matching the persona in CHAT_SYSTEM_PROMPT"
    - "Both profiles have extraction_source='manual' and extraction_confidence='high'"
    - "Coach assignments exist for Shriram (bikes domain) and Venki (general, is_default=TRUE)"
    - "Seed script is idempotent -- running twice produces the same result"
    - "CHAT_SYSTEM_PROMPT constant in openai_coach.py is NOT modified"
  artifacts:
    - path: "scripts/seed_coaching_profiles.py"
      provides: "Idempotent seed script for Shriram and Venki profiles + coach assignments"
      contains: "SHRIRAM_PROFILE"
    - path: "tests/test_coaching_models.py"
      provides: "Seed validation tests (unskipped and passing)"
      contains: "def test_seed_shriram_profile"
  key_links:
    - from: "scripts/seed_coaching_profiles.py"
      to: "personality_profile and coach_assignment tables"
      via: "raw psycopg2 INSERT ... ON CONFLICT using DATABASE_URL"
      pattern: "ON CONFLICT.*rider_id.*profile_type"
    - from: "scripts/seed_coaching_profiles.py"
      to: "services/openai_coach.py"
      via: "seed data derived from CHAT_SYSTEM_PROMPT content (not imported)"
      pattern: "SHRIRAM_PROFILE.*tone.*direct"
---

<objective>
Seed Shriram and Venki personality profiles and coach assignments from the existing CHAT_SYSTEM_PROMPT content.

Purpose: Make the system testable with real coach persona data before extraction pipelines exist (Phase 8). The seed data preserves existing chatbot behavior by translating the hardcoded prompt personas into structured database rows.
Output: Idempotent seed script and passing seed validation tests.
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-foundation/07-RESEARCH.md
@.planning/phases/07-data-foundation/07-01-SUMMARY.md
@.planning/phases/07-data-foundation/07-02-SUMMARY.md

@services/openai_coach.py
@models.py
@tests/test_coaching_models.py

<interfaces>
<!-- CRUD functions created in Plan 02 that the seed script will use -->
From models.py (Plan 02 output):
```python
def upsert_personality_profile(rider_id, profile_type, fields, updated_by='system'):
    """Insert or update personality profile. fields dict maps column names to values."""

def upsert_coach_assignment(coach_rider_id, topic_domain, fields, updated_by='system'):
    """Insert or update coach assignment. fields dict maps column names to values."""

def get_personality_profile(rider_id, profile_type='coach'):
    """Get active personality profile for a rider. NOT CACHED."""
```

<!-- Seed data values from research -->
Shriram (from CHAT_SYSTEM_PROMPT):
- tone='direct', humor_type='dry', directness='high'
- topics_allowed=['bike','gear','maintenance','components','fit','wheels']
- topic_biases=['bikes','accessories','components','gear']
- signature_phrases=['recognizes riders by their bikes','loves gear upgrades']
- extraction_source='manual', extraction_confidence='high'

Venki (from CHAT_SYSTEM_PROMPT):
- tone='playful', humor_type='sarcastic', directness='medium'
- topics_allowed=['training','nutrition','randonneuring','strategy','general']
- topic_biases=['training philosophy','mental game','nutrition']
- signature_phrases=['tongue-in-cheek','guide figure']
- extraction_source='manual', extraction_confidence='high'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent seed script for Shriram and Venki profiles</name>
  <files>scripts/seed_coaching_profiles.py</files>
  <action>
Create `scripts/seed_coaching_profiles.py` as a standalone script (no Flask context required) that seeds personality profiles and coach assignments for Shriram and Venki.

The script must:

1. **Look up rider IDs by name** (not hardcoded IDs) -- `SELECT id FROM rider WHERE first_name = 'Shriram'` and `SELECT id FROM rider WHERE first_name ILIKE 'venk%'` (Venki may be stored as Venkatesh or similar). If either rider is not found, print a warning and skip that rider (do not crash).

2. **Connect directly via psycopg2** using the `get_database_url()` pattern from `apply_migration_standalone.py` (read DATABASE_URL from .env or env var). Do NOT import from Flask app or models.py -- this is a standalone script.

3. **Upsert personality profiles** using `INSERT ... ON CONFLICT (rider_id, profile_type) DO UPDATE SET` so the script is idempotent. Profile data:

   Shriram profile_type='coach':
   - tone='direct', humor_type='dry', directness='high'
   - encouragement_style='data-driven', technical_depth='expert'
   - signature_phrases: ['recognizes riders by their bikes', 'loves gear upgrades', 'opinionated about components']
   - topic_biases: ['bikes', 'accessories', 'components', 'gear', 'fit']
   - topics_allowed: ['bike', 'gear', 'maintenance', 'components', 'fit', 'wheels', 'tires', 'lighting']
   - extraction_source='manual', extraction_date=today, extraction_confidence='high'
   - updated_by='seed_script'

   Venki profile_type='coach':
   - tone='playful', humor_type='sarcastic', directness='medium'
   - encouragement_style='balanced', technical_depth='expert'
   - signature_phrases: ['tongue-in-cheek humor', 'big-picture thinker', 'storyteller']
   - topic_biases: ['training philosophy', 'mental game', 'nutrition', 'randonneuring strategy']
   - topics_allowed: ['training', 'nutrition', 'randonneuring', 'strategy', 'pacing', 'mental', 'general']
   - extraction_source='manual', extraction_date=today, extraction_confidence='high'
   - updated_by='seed_script'

4. **Upsert coach assignments**:
   - Shriram: topic_domain='bikes', is_default=FALSE, is_active=TRUE
   - Shriram: topic_domain='gear', is_default=FALSE, is_active=TRUE
   - Shriram: topic_domain='maintenance', is_default=FALSE, is_active=TRUE
   - Venki: topic_domain='training', is_default=FALSE, is_active=TRUE
   - Venki: topic_domain='nutrition', is_default=FALSE, is_active=TRUE
   - Venki: topic_domain='general', is_default=TRUE, is_active=TRUE (fallback coach)
   - Venki: topic_domain='randonneuring', is_default=FALSE, is_active=TRUE

5. **Print summary**: "Seeded N profiles, M coach assignments" with per-item status (created/updated).

6. **Do NOT modify `services/openai_coach.py`** -- the CHAT_SYSTEM_PROMPT constant must remain in place until Phase 9.

Read `services/openai_coach.py` CHAT_SYSTEM_PROMPT content to verify the seed values accurately translate the prose descriptions. If the prompt mentions additional personality traits not listed above, include them in the seed data.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/seed_coaching_profiles.py').read()); print('syntax ok')" && grep -q "ON CONFLICT" scripts/seed_coaching_profiles.py && echo "idempotent" && grep -q "SHRIRAM" scripts/seed_coaching_profiles.py && echo "has shriram" && grep -q "VENKI" scripts/seed_coaching_profiles.py && echo "has venki"</automated>
  </verify>
  <done>Seed script is syntactically valid Python, uses ON CONFLICT for idempotency, looks up riders by name (not hardcoded IDs), seeds both Shriram and Venki profiles + coach assignments, and does not modify openai_coach.py.</done>
</task>

<task type="auto">
  <name>Task 2: Unskip and implement seed validation tests</name>
  <files>tests/test_coaching_models.py</files>
  <action>
In `tests/test_coaching_models.py`, unskip `test_seed_shriram_profile` and `test_seed_venki_profile` and implement them:

1. `test_seed_shriram_profile(db_conn)` -- Use raw SQL to query personality_profile for Shriram's rider_id with profile_type='coach'. Verify:
   - tone is 'direct'
   - humor_type is 'dry'
   - directness is 'high'
   - extraction_source is 'manual'
   - extraction_confidence is 'high'
   - topics_allowed is a non-empty list containing 'bike' and 'gear'
   - signature_phrases is a non-empty list

   Also verify coach_assignment rows exist for Shriram with topic_domain IN ('bikes', 'gear', 'maintenance').

   NOTE: These tests require the seed script to have been run against the test DB first. If the profile does not exist, use `pytest.skip('Seed data not yet applied -- run scripts/seed_coaching_profiles.py first')`.

2. `test_seed_venki_profile(db_conn)` -- Same pattern for Venki:
   - tone is 'playful'
   - humor_type is 'sarcastic'
   - directness is 'medium'
   - extraction_source is 'manual'
   - topics_allowed contains 'training' and 'general'
   - coach_assignment rows exist with is_default=TRUE for 'general' domain

3. `test_seed_idempotency(db_conn)` -- NEW test: Insert a profile via raw SQL, then verify that running the seed upsert SQL again does not create a duplicate (count stays at 1 for the rider_id + profile_type combo).

4. `test_existing_tests_unbroken()` -- NEW test: Simply import CHAT_SYSTEM_PROMPT from services.openai_coach and assert it is a non-empty string. This confirms the constant was not removed.

Also verify `pytest tests/test_system_prompt.py -x` still passes (no modification to that file).
  </action>
  <verify>
    <automated>cd /Users/msambhus/LocalDocuments/Personal/TeamAshaRandonneuring && pytest tests/test_coaching_models.py -x -v -k "not seed_shriram and not seed_venki" 2>&1 | tail -20</automated>
  </verify>
  <done>Seed validation tests are implemented. test_existing_tests_unbroken passes confirming CHAT_SYSTEM_PROMPT is intact. Seed-specific tests are runnable after seed script execution. All non-seed tests continue to pass.</done>
</task>

</tasks>

<verification>
- `python3 -c "import ast; ast.parse(open('scripts/seed_coaching_profiles.py').read())"` -- seed script valid
- `grep -q "ON CONFLICT" scripts/seed_coaching_profiles.py` -- idempotent upserts
- `pytest tests/test_coaching_models.py -x -v` -- all non-skip tests pass
- `pytest tests/test_system_prompt.py -x` -- existing tests still pass
- `grep -q "CHAT_SYSTEM_PROMPT" services/openai_coach.py` -- constant not removed
</verification>

<success_criteria>
- Seed script creates Shriram and Venki coach profiles with structured typed fields matching CHAT_SYSTEM_PROMPT content
- Seed script creates coach_assignment rows for both coaches with correct topic domains
- Venki is marked as fallback coach (is_default=TRUE on 'general' domain)
- Script is idempotent (ON CONFLICT DO UPDATE)
- Script looks up riders by name, not hardcoded IDs
- CHAT_SYSTEM_PROMPT constant in openai_coach.py is NOT modified
- Seed validation tests are implemented and runnable
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-foundation/07-03-SUMMARY.md`
</output>
