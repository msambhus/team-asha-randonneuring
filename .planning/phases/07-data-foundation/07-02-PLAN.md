---
phase: 07-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - models.py
  - tests/test_coaching_models.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-05
  - GUARD-01

must_haves:
  truths:
    - "CRUD functions for all four tables exist in models.py and work with parameterized queries"
    - "get_personality_profile returns a dict with all typed fields when profile exists, None when not"
    - "upsert_personality_profile inserts new profiles and updates existing ones atomically"
    - "Guardrail CRUD functions filter by deleted_at IS NULL and respect is_active flag"
    - "No admin data is cached -- edits are immediately visible"
  artifacts:
    - path: "models.py"
      provides: "CRUD functions for personality_profile, gear_preference, coach_assignment, coaching_guardrail"
      contains: "def get_personality_profile"
    - path: "tests/test_coaching_models.py"
      provides: "CRUD function tests (unskipped and passing)"
      contains: "def test_crud_personality_profile"
  key_links:
    - from: "models.py"
      to: "personality_profile table"
      via: "_execute() with parameterized SQL"
      pattern: "_execute.*personality_profile"
    - from: "models.py"
      to: "coaching_guardrail table"
      via: "_execute() with parameterized SQL"
      pattern: "_execute.*coaching_guardrail"
    - from: "tests/test_coaching_models.py"
      to: "models.py"
      via: "import and call CRUD functions"
      pattern: "from models import"
---

<objective>
Add CRUD functions to models.py for all four new tables, and fill in the CRUD test stubs from Plan 01.

Purpose: Provide the data access layer that seed scripts (Plan 03), extraction scripts (Phase 8), chat integration (Phase 9), and admin UI (Phase 10) will all use. Without these functions, no code can read or write personality/coaching data.
Output: Extended models.py with 10-12 new functions, passing CRUD tests.
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-foundation/07-RESEARCH.md
@.planning/phases/07-data-foundation/07-01-SUMMARY.md

@models.py
@tests/test_coaching_models.py
@tests/conftest.py

<interfaces>
<!-- Existing models.py patterns the executor must follow exactly -->
From models.py:
```python
from db import get_db
from cache import cache, CACHE_TIMEOUT
import psycopg2.extras

def _execute(sql, params=None):
    """Execute SQL with RealDictCursor. Returns cursor."""
    conn = get_db()
    cur = conn.cursor(cursor_factory=psycopg2.extras.RealDictCursor)
    cur.execute(sql, params)
    return cur
```

<!-- Table schemas from Plan 01 migration -->
personality_profile: id, rider_id, profile_type, tone, humor_type, directness, signature_phrases (TEXT[]), topic_biases (TEXT[]), topics_allowed (TEXT[]), preferred_formality, humor_sensitivity, encouragement_style, technical_depth, extraction_source, extraction_date, source_message_count, extraction_confidence, created_at, updated_at, updated_by, deleted_at
  UNIQUE (rider_id, profile_type)

gear_preference: id, rider_id (UNIQUE), bike_make, bike_model, bike_year, bike_material, wheels_tires, lighting, bags, navigation, kit, value_orientation, created_at, updated_at, updated_by, deleted_at

coach_assignment: id, coach_rider_id, topic_domain, is_default, is_active, created_at, updated_at, updated_by, deleted_at
  UNIQUE (coach_rider_id, topic_domain)

coaching_guardrail: id, rule_type, rule_value, applies_to, is_active, rule_version, created_at, updated_at, updated_by, deleted_at
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CRUD functions for all four tables to models.py</name>
  <files>models.py, tests/test_coaching_models.py</files>
  <behavior>
    - test_crud_personality_profile: upsert a coach profile, read it back, verify all fields match. Update one field, verify change persisted. Soft-delete, verify get returns None.
    - test_crud_guardrail: insert a guardrail, read it back. Update rule_value, verify rule_version incremented. Toggle is_active, verify filter works. Soft-delete, verify excluded from active list.
    - test_crud_gear_preference: upsert gear for a rider, read back, verify bike fields. Update, verify change.
    - test_crud_coach_assignment: insert assignments, list by coach. List active assignments for a domain. Toggle is_active.
  </behavior>
  <action>
First, unskip and flesh out the CRUD test stubs in `tests/test_coaching_models.py` (remove `pytest.mark.skip`). Write full test implementations for test_crud_personality_profile and test_crud_guardrail per the behavior above. Add test_crud_gear_preference and test_crud_coach_assignment tests.

Tests must import from models: `from models import get_personality_profile, upsert_personality_profile, soft_delete_personality_profile, get_gear_preference, upsert_gear_preference, get_coach_assignments, upsert_coach_assignment, get_active_guardrails, insert_guardrail, update_guardrail, soft_delete_guardrail`.

Tests use the `app` fixture (for Flask context so get_db() works) plus `db_conn` for direct verification. Each test must work within a transaction that rolls back.

Then add the following functions to models.py under a new section `# ========== PERSONALITY & COACHING ==========`:

**personality_profile functions:**
1. `get_personality_profile(rider_id, profile_type='coach')` -- SELECT * WHERE rider_id=%s AND profile_type=%s AND deleted_at IS NULL. Returns dict or None. NOT cached (admin edits must be immediate).
2. `get_all_personality_profiles(profile_type=None)` -- SELECT all active profiles, optionally filtered by type. Returns list.
3. `upsert_personality_profile(rider_id, profile_type, fields, updated_by='system')` -- INSERT ... ON CONFLICT (rider_id, profile_type) DO UPDATE SET. The `fields` param is a dict of column->value. Must call `conn.commit()`. Use parameterized queries only.
4. `soft_delete_personality_profile(profile_id, updated_by='system')` -- SET deleted_at=NOW(), updated_by=%s WHERE id=%s. Must call `conn.commit()`.

**gear_preference functions:**
5. `get_gear_preference(rider_id)` -- SELECT * WHERE rider_id=%s AND deleted_at IS NULL. Returns dict or None. NOT cached.
6. `upsert_gear_preference(rider_id, fields, updated_by='system')` -- INSERT ... ON CONFLICT (rider_id) DO UPDATE SET. Must call `conn.commit()`.

**coach_assignment functions:**
7. `get_coach_assignments(coach_rider_id=None, topic_domain=None, active_only=True)` -- SELECT with optional filters. Always filters deleted_at IS NULL. If active_only, also filters is_active=TRUE. Returns list.
8. `upsert_coach_assignment(coach_rider_id, topic_domain, fields, updated_by='system')` -- INSERT ... ON CONFLICT (coach_rider_id, topic_domain) DO UPDATE SET. Must call `conn.commit()`.

**coaching_guardrail functions:**
9. `get_active_guardrails(rule_type=None, applies_to=None)` -- SELECT WHERE is_active=TRUE AND deleted_at IS NULL, with optional rule_type and applies_to filters. Returns list.
10. `insert_guardrail(rule_type, rule_value, applies_to='all', updated_by='system')` -- INSERT with all required fields. Must call `conn.commit()`. Returns the inserted row.
11. `update_guardrail(guardrail_id, fields, updated_by='system')` -- UPDATE SET for provided fields. The DB trigger handles rule_version increment. Must call `conn.commit()`.
12. `soft_delete_guardrail(guardrail_id, updated_by='system')` -- SET deleted_at=NOW(). Must call `conn.commit()`.

CRITICAL rules from research:
- Do NOT use `@cache.memoize` on any of these functions -- admin data must be immediately visible
- Always include `WHERE deleted_at IS NULL` in every SELECT
- Always call `get_db().commit()` after DML (autocommit is False)
- Use `_execute()` for reads (returns RealDictCursor)
- For writes, get conn via `get_db()`, create cursor, execute, commit
- Use `%s` parameterized queries only -- never f-strings for SQL values
- TEXT[] columns (signature_phrases, topic_biases, topics_allowed) are passed as Python lists -- psycopg2 handles adaptation via RealDictCursor
  </action>
  <verify>
    <automated>cd /Users/msambhus/LocalDocuments/Personal/TeamAshaRandonneuring && pytest tests/test_coaching_models.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All 12 CRUD tests pass. models.py has 12 new functions under the PERSONALITY & COACHING section. No existing functions modified. No caching decorators on new functions. All queries use parameterized SQL with deleted_at IS NULL filter.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_coaching_models.py -x -v` -- all tests pass (schema + CRUD)
- `pytest tests/test_system_prompt.py -x` -- existing tests still pass
- `grep -c "def get_personality_profile\|def upsert_personality_profile\|def get_active_guardrails\|def insert_guardrail" models.py` returns 4
- `grep -c "@cache.memoize" models.py` for new functions returns 0 (no caching on coaching functions)
</verification>

<success_criteria>
- 12 new CRUD functions in models.py under PERSONALITY & COACHING section
- All functions use parameterized queries via _execute() or get_db() cursor
- No @cache.memoize on any new function
- All SELECT queries include WHERE deleted_at IS NULL
- All write functions call conn.commit()
- CRUD tests pass (unskipped from Plan 01 stubs + new tests)
- Existing test suite unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-foundation/07-02-SUMMARY.md`
</output>
