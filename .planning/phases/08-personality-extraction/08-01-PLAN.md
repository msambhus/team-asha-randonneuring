---
phase: 08-personality-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/012_personality_extraction_fields.sql
  - migrations/apply_migration_012.py
  - tests/test_personality_extraction.py
  - requirements-dev.txt
autonomous: true
requirements: [EXTR-01, EXTR-03, EXTR-04, EXTR-05, EXTR-06]

must_haves:
  truths:
    - "personality_profile table has response_length_tendency, question_asking_behavior, and domain_bias columns"
    - "personality_trait_evidence table exists with rider_id, trait_name, source_quote, extraction_source columns"
    - "extraction_source CHECK constraint allows 'merged' in addition to whatsapp/blog/manual"
    - "UNIQUE constraint on personality_profile is (rider_id, profile_type, extraction_source) to allow per-source rows"
    - "Test scaffolds exist for all extraction logic: noise filtering, confidence, model fields, merge, evidence storage, extract_from_messages"
    - "trafilatura and pdfplumber are in requirements-dev.txt"
  artifacts:
    - path: "migrations/012_personality_extraction_fields.sql"
      provides: "Schema additions for extraction fields and evidence table"
      contains: "personality_trait_evidence"
    - path: "migrations/apply_migration_012.py"
      provides: "Standalone migration apply script"
      contains: "012_personality_extraction_fields"
    - path: "tests/test_personality_extraction.py"
      provides: "Test scaffolds for all EXTR requirements"
      min_lines: 90
    - path: "requirements-dev.txt"
      provides: "Dev-only dependencies for extraction scripts"
      contains: "trafilatura"
  key_links:
    - from: "migrations/012_personality_extraction_fields.sql"
      to: "personality_profile"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "ALTER TABLE personality_profile"
    - from: "tests/test_personality_extraction.py"
      to: "extraction logic modules"
      via: "import and test"
      pattern: "def test_"
---

<objective>
Create the schema migration, test scaffolds, and dev dependencies needed before extraction scripts can run.

Purpose: Plan 01 is the foundation wave. Extraction scripts (Plans 02 and 03) cannot function without the missing DB columns, evidence table, relaxed UNIQUE constraint, and test infrastructure.
Output: Migration SQL, apply script, test file with all stubs, updated requirements-dev.txt.
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-personality-extraction/08-RESEARCH.md
@.planning/phases/08-personality-extraction/08-VALIDATION.md

<interfaces>
<!-- Existing schema from migrations/011_personality_coaching_tables.sql -->
<!-- personality_profile has UNIQUE (rider_id, profile_type) — must be changed to (rider_id, profile_type, extraction_source) -->
<!-- extraction_source CHECK currently: ('whatsapp', 'blog', 'manual') — must add 'merged' -->
<!-- Missing columns: response_length_tendency, question_asking_behavior, domain_bias -->

From models.py (lines 2502-2526):
```python
def upsert_personality_profile(rider_id, profile_type, fields, updated_by='system'):
    """Insert or update personality profile. fields dict maps column names to values."""
    # Uses ON CONFLICT (rider_id, profile_type) DO UPDATE SET ...
```

From scripts/whatsapp_parser.py:
```python
MEDIA_SKIP_PATTERNS = [...]  # line 47
def parse_export(filepath):  # line 57 — returns list of message dicts
```

From scripts/seed_coaching_profiles.py:
```python
def lookup_rider(cur, first_name_pattern):  # line 93 — ILIKE lookup by name
def upsert_profile(cur, rider_id, profile_data):  # line 105 — ON CONFLICT upsert
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 012 — add extraction columns, evidence table, fix constraints</name>
  <files>migrations/012_personality_extraction_fields.sql, migrations/apply_migration_012.py</files>
  <action>
Create `migrations/012_personality_extraction_fields.sql` with:

1. **Add 3 missing columns** to personality_profile (per EXTR-03):
   - `response_length_tendency VARCHAR(10) CHECK (response_length_tendency IN ('brief', 'moderate', 'verbose'))`
   - `question_asking_behavior VARCHAR(15) CHECK (question_asking_behavior IN ('rarely', 'sometimes', 'frequently'))`
   - `domain_bias VARCHAR(100)` — free-text short phrase, app-layer sanitized
   Use `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for idempotency.

2. **Fix extraction_source CHECK** to add 'merged':
   - `ALTER TABLE personality_profile DROP CONSTRAINT IF EXISTS personality_profile_extraction_source_check;`
   - `ALTER TABLE personality_profile ADD CONSTRAINT personality_profile_extraction_source_check CHECK (extraction_source IN ('whatsapp', 'blog', 'manual', 'merged'));`

3. **Fix UNIQUE constraint** from `(rider_id, profile_type)` to `(rider_id, profile_type, extraction_source)`:
   The current constraint prevents storing separate whatsapp/blog/merged rows for the same rider. Extraction needs per-source rows so the merge script can read both sources independently.
   - `ALTER TABLE personality_profile DROP CONSTRAINT IF EXISTS personality_profile_rider_id_profile_type_key;`
   - `ALTER TABLE personality_profile ADD CONSTRAINT personality_profile_rider_id_profile_type_source_key UNIQUE (rider_id, profile_type, extraction_source);`

4. **Create personality_trait_evidence table** (per EXTR-04):
   ```sql
   CREATE TABLE IF NOT EXISTS personality_trait_evidence (
       id SERIAL PRIMARY KEY,
       rider_id INTEGER NOT NULL REFERENCES rider(id) ON DELETE CASCADE,
       trait_name VARCHAR(50) NOT NULL,
       source_quote TEXT NOT NULL,
       extraction_source VARCHAR(10) NOT NULL CHECK (extraction_source IN ('whatsapp', 'blog', 'merged')),
       created_at TIMESTAMPTZ DEFAULT NOW(),
       updated_at TIMESTAMPTZ DEFAULT NOW()
   );
   CREATE INDEX IF NOT EXISTS idx_trait_evidence_rider ON personality_trait_evidence(rider_id);
   CREATE INDEX IF NOT EXISTS idx_trait_evidence_rider_trait ON personality_trait_evidence(rider_id, trait_name);
   ```

5. **Create apply_migration_012.py** following the exact pattern of `migrations/apply_migration_012.py` (same as `apply_migration_011.py`): load DATABASE_URL from .env, read the SQL file, execute it, print success/failure. Copy structure from `apply_migration_011.py`.

IMPORTANT: After changing the UNIQUE constraint, the existing `upsert_personality_profile()` in models.py uses `ON CONFLICT (rider_id, profile_type)`. This function must be updated to use `ON CONFLICT (rider_id, profile_type, extraction_source)`. Update models.py lines 2516-2521 to include `extraction_source` in the conflict target. Also ensure the seed script's `upsert_profile()` still works — the seeded profiles have `extraction_source='manual'` so the new 3-column unique will still work for those.
  </action>
  <verify>
    <automated>python -c "
import pathlib
sql = pathlib.Path('migrations/012_personality_extraction_fields.sql').read_text()
assert 'response_length_tendency' in sql
assert 'question_asking_behavior' in sql
assert 'domain_bias' in sql
assert 'personality_trait_evidence' in sql
assert 'merged' in sql
assert pathlib.Path('migrations/apply_migration_012.py').exists()
print('Migration files valid')
"</automated>
  </verify>
  <done>Migration SQL adds 3 columns, fixes extraction_source CHECK to include 'merged', changes UNIQUE constraint to (rider_id, profile_type, extraction_source), creates personality_trait_evidence table. Apply script exists. models.py upsert_personality_profile updated for new conflict target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds and install dev dependencies</name>
  <files>tests/test_personality_extraction.py, requirements-dev.txt</files>
  <behavior>
    - test_extraction_model_fields: PersonalityExtraction Pydantic model has all 8 required fields (tone, humor_type, directness, encouragement_style, domain_bias, response_length_tendency, question_asking_behavior, signature_phrases) plus evidence fields
    - test_group_by_sender_filters_noise: group_by_sender() filters system messages, media skip patterns, short reactions (< 3 words), URL-only messages
    - test_compute_confidence: compute_confidence(50+) = 'high', compute_confidence(20-49) = 'medium', compute_confidence(<20) = 'low'
    - test_extract_from_messages_returns_model: extract_from_messages() calls client.chat.completions.parse() with PersonalityExtraction response_format and returns a PersonalityExtraction instance (mock the OpenAI client)
    - test_merge_profiles_blog_wins: merge_profiles() uses blog value for enum fields when both sources present; unions signature_phrases capped at 5; uses lower confidence
    - test_fetch_blog_text_from_url: fetch_blog_text() calls trafilatura and returns text (mock HTTP)
    - test_extract_pdf_text: extract_pdf_text() reads PDF pages and returns concatenated text (use a tiny test PDF fixture)
    - test_evidence_quotes_stored: After extraction, personality_trait_evidence has 3-5 rows per trait (integration test stub — marked with @pytest.mark.integration)
    - test_migration_012_columns: personality_profile has the 3 new columns (integration test stub — marked with @pytest.mark.integration)
    - test_trait_evidence_schema: personality_trait_evidence table exists with correct columns (integration test stub — marked with @pytest.mark.integration)
  </behavior>
  <action>
1. **Update requirements-dev.txt**: Add `trafilatura==2.0.0` and `pdfplumber==0.11.9` to the file. Preserve existing entries. Then run `pip install -r requirements-dev.txt`.

2. **Create tests/test_personality_extraction.py** with all test functions listed in the behavior block. For unit tests, write the actual test logic with assertions against the helper functions that will be created in Plans 02 and 03. For now, import from a module path that Plans 02/03 will create — the tests should RED (fail with ImportError) until the implementations exist. This is the Wave 0 test scaffold.

Structure the imports to come from:
- `scripts.personality_helpers` — for `group_by_sender`, `compute_confidence`, `merge_profiles`, `extract_from_messages`, `PersonalityExtraction`, `TraitEvidence`
- `scripts.extract_personality_blog` — for `fetch_blog_text`, `extract_pdf_text`

For `test_group_by_sender_filters_noise`: Create a list of fake messages mimicking whatsapp_parser output format (`{'sender': 'Venki', 'content': '...', 'is_system': False, 'ts': datetime}`), including noise messages. Assert that group_by_sender returns only qualifying messages.

For `test_compute_confidence`: Test all 3 threshold boundaries: 50, 20, 19.

For `test_extract_from_messages_returns_model`: Create a mock OpenAI client where `client.chat.completions.parse()` returns a mock response with `response.choices[0].message.parsed` set to a valid PersonalityExtraction instance. Call `extract_from_messages(client, messages, 'TestSender')` and assert the return value is a PersonalityExtraction instance. Verify `client.chat.completions.parse` was called with `response_format=PersonalityExtraction` and `temperature=0`.

For `test_merge_profiles_blog_wins`: Create two dicts (whatsapp_row, blog_row) with conflicting enum values. Assert blog wins on enums, phrases are unioned and capped at 5.

Mark integration tests with `@pytest.mark.integration` and add `pytest.ini` marker registration if not already present.

For `test_fetch_blog_text_from_url`: Use `unittest.mock.patch` to mock `trafilatura.fetch_url` and `trafilatura.extract`.

For `test_extract_pdf_text`: Create a minimal test PDF fixture using `reportlab` or check if pdfplumber ships test data. Simpler approach: mock `pdfplumber.open` with a mock that returns pages with `extract_text()`.
  </action>
  <verify>
    <automated>python -c "
import ast, pathlib
tree = ast.parse(pathlib.Path('tests/test_personality_extraction.py').read_text())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
required = [
    'test_extraction_model_fields',
    'test_group_by_sender_filters_noise',
    'test_compute_confidence',
    'test_extract_from_messages_returns_model',
    'test_merge_profiles_blog_wins',
    'test_fetch_blog_text_from_url',
    'test_extract_pdf_text',
    'test_evidence_quotes_stored',
    'test_migration_012_columns',
    'test_trait_evidence_schema',
]
missing = [r for r in required if r not in funcs]
assert not missing, f'Missing tests: {missing}'
print(f'All {len(required)} test functions present')
"</automated>
  </verify>
  <done>tests/test_personality_extraction.py exists with all 10 test functions. requirements-dev.txt includes trafilatura==2.0.0 and pdfplumber==0.11.9. Unit tests define expected behavior for extraction helpers including extract_from_messages end-to-end (with mocked LLM). Integration tests are marked and stubbed.</done>
</task>

</tasks>

<verification>
- `migrations/012_personality_extraction_fields.sql` exists and contains all schema changes
- `migrations/apply_migration_012.py` exists and follows project pattern
- `tests/test_personality_extraction.py` has 10 test functions covering all EXTR requirements
- `requirements-dev.txt` contains trafilatura and pdfplumber
- models.py upsert conflict target updated for new UNIQUE constraint
</verification>

<success_criteria>
- Migration SQL is idempotent (IF NOT EXISTS / IF EXISTS patterns)
- All 10 test function stubs exist and are parseable Python
- Dev dependencies installable via pip
- Existing seed_coaching_profiles.py still works against updated schema (manual profiles have extraction_source='manual')
</success_criteria>

<output>
After completion, create `.planning/phases/08-personality-extraction/08-01-SUMMARY.md`
</output>
