---
phase: 03-forecast-wind-in-base-ride-plan
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - routes/riders.py
  - templates/ride_plan_detail.html
autonomous: false
requirements: [BPLN-01, BPLN-02, BPLN-03, BPLN-04, BPLN-05, BPLN-06]

must_haves:
  truths:
    - "Rider viewing a base ride plan with an RWGPS route sees a Wind column with color-coded wind speed at each stop"
    - "Wind cells show green for tailwind, red for headwind, blue for crosswind with opacity varying by speed"
    - "Wind cell text is smaller for light winds and larger for strong winds"
    - "A ride plan without a linked RWGPS route shows no Wind column and no error"
    - "A wind legend below the table explains the green/red/blue color coding"
    - "Viewing the same ride plan twice within an hour does not trigger a second Open-Meteo API call"
  artifacts:
    - path: "routes/riders.py"
      provides: "fetch_stop_wind() call in ride_plan_detail() with try/except"
      contains: "fetch_stop_wind"
    - path: "templates/ride_plan_detail.html"
      provides: "Wind column in plan-table and wind legend block"
      contains: "stop_wind"
  key_links:
    - from: "routes/riders.py:ride_plan_detail"
      to: "services/weather.py:fetch_stop_wind"
      via: "function call with try/except"
      pattern: "fetch_stop_wind\\("
    - from: "routes/riders.py:ride_plan_detail"
      to: "templates/ride_plan_detail.html"
      via: "render_template stop_wind parameter"
      pattern: "stop_wind=stop_wind"
    - from: "templates/ride_plan_detail.html"
      to: "stop_wind list"
      via: "Jinja2 loop.index0 indexing"
      pattern: "stop_wind\\[loop\\.index0\\]"
---

<objective>
Wire fetch_stop_wind() into the base ride plan route handler and add the Wind column + legend to the plan-table template.

Purpose: This is the user-facing delivery — riders see color-coded wind at every stop in the table view. All the math and data pipeline exist from Plans 01-01/02-01/03-01; this plan connects them to the page.

Output: Wind column visible in ride plan detail table view, legend below table, graceful degradation when no RWGPS route
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forecast-wind-in-base-ride-plan/03-RESEARCH.md
@.planning/phases/03-forecast-wind-in-base-ride-plan/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From services/weather.py (created in Plan 03-01):
```python
def fetch_stop_wind(stops, track_points, plan_slug, start_time_str, cache=None):
    """Return per-stop wind data list for display in the base ride plan table.

    Returns list of dicts (same length as stops):
    {
        'wind_speed_kmh': float,
        'wind_type': str,        # 'headwind'|'tailwind'|'crosswind'
        'style': dict,           # {'color': str, 'background': str, 'font_size': str}
        'label': str,            # e.g. 'strong headwind'
    }
    Entries may be None for unresolvable stops.
    Returns None on any error or when track_points is empty.
    """
```

From services/rwgps.py:
```python
def fetch_route(route_id):
    """Returns route dict. route['track_points'] = list of {y, x, d, e} dicts.
    Raises on non-200 response."""
```

From routes/riders.py (current render_template call at line 1354):
```python
return render_template('ride_plan_detail.html',
    plan=plan,
    stops=stops,
    ...
    weather_route_id=weather_route_id,
    ...)
```

From templates/ride_plan_detail.html (table structure, lines 1458-1559):
- plan-table has columns: #, Type, Location, Seg.Dist, Cumul.Dist, Elev.Gain, Ft/Mi, Seg.Time, Avg Speed, [Time Bank], Diff., Notes
- Conditional column pattern: `{% if plan.cutoff_hours %}<th>...</th>{% endif %}`
- Wind column goes AFTER Avg Speed and BEFORE Time Bank (or before Diff. if no cutoff)
- tfoot row has matching conditional: `{% if plan.cutoff_hours %}<td></td>{% endif %}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route handler integration — call fetch_stop_wind() and pass to template</name>
  <files>routes/riders.py</files>
  <action>
    In routes/riders.py `ride_plan_detail()` function (line ~1352, just before the render_template call):

    1. Add import at top of file: `from services.weather import fetch_stop_wind`
    2. Add import if not present: `from services.rwgps import fetch_route`
    3. Add import if not present: `from cache import cache`

    4. Insert wind fetch block AFTER `stops, use_timeline = _attach_break_metadata(stops)` (line 1352) and BEFORE `return render_template(...)` (line 1354):

    ```python
    # Wind data for table view
    stop_wind = None
    if weather_route_id:
        try:
            route_data = fetch_route(weather_route_id)
            track_points = route_data.get('track_points') or []
            stop_wind = fetch_stop_wind(
                stops=stops,
                track_points=track_points,
                plan_slug=plan['slug'],
                start_time_str=plan.get('start_time', '06:00'),
                cache=cache,
            )
        except Exception:
            app.logger.exception("Wind fetch failed for plan %s", slug)
            stop_wind = None
    ```

    5. Add `stop_wind=stop_wind` to the render_template() kwargs (after weather_route_id).

    IMPORTANT: Use `app.logger.exception()` not `print()` per project conventions. The try/except must catch ALL exceptions — RWGPS and Open-Meteo are external APIs. The page MUST render without wind if anything fails (BPLN-05 graceful degradation).

    NOTE: `fetch_route` may already be imported elsewhere in riders.py. Check before adding a duplicate import. Same for `cache`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -x -q</automated>
  </verify>
  <done>ride_plan_detail() passes stop_wind to template; full test suite passes with no regressions; page renders without error when weather_route_id is None</done>
</task>

<task type="auto">
  <name>Task 2: Template — Wind column in plan-table and wind legend</name>
  <files>templates/ride_plan_detail.html</files>
  <action>
    Modify templates/ride_plan_detail.html to add the Wind column and legend. ALL wind markup is guarded by `{% if stop_wind %}`. Use INLINE STYLES only — no Tailwind dynamic classes (locked project decision).

    **A. Table header (line ~1470, after Avg Speed th, before the cutoff_hours conditional):**
    ```html
    {% if stop_wind %}<th style="text-align:center;">Wind</th>{% endif %}
    ```

    **B. Table body cell (inside the {% for s in stops %} loop, line ~1515, after avg_speed td, before the cutoff_hours conditional):**
    ```html
    {% if stop_wind %}
    {% set w = stop_wind[loop.index0] %}
    <td style="text-align:center;padding:4px 8px;">
      {% if w %}
      <span style="display:inline-block;padding:2px 6px;border-radius:4px;background:{{ w.style.background }};color:{{ w.style.color }};font-size:{{ w.style.font_size }};font-weight:600;white-space:nowrap;">
        {{ w.wind_speed_kmh }} km/h
      </span>
      {% else %}
      &mdash;
      {% endif %}
    </td>
    {% endif %}
    ```

    **C. Table footer (line ~1554, after avg_elapsed_speed td, before the cutoff_hours conditional):**
    ```html
    {% if stop_wind %}<td></td>{% endif %}
    ```

    **D. Wind legend block (after the closing `</div>` of table-view div at line ~1561, before the Back link section):**
    ```html
    {% if stop_wind %}
    <div style="margin-top:12px;display:flex;gap:16px;align-items:center;flex-wrap:wrap;font-size:0.75rem;color:var(--text-light);">
      <span style="font-weight:600;color:var(--text);">Wind legend:</span>
      <span>
        <span style="display:inline-block;width:12px;height:12px;border-radius:2px;background:#16A34A;vertical-align:middle;margin-right:3px;"></span>
        Tailwind
      </span>
      <span>
        <span style="display:inline-block;width:12px;height:12px;border-radius:2px;background:#DC2626;vertical-align:middle;margin-right:3px;"></span>
        Headwind
      </span>
      <span>
        <span style="display:inline-block;width:12px;height:12px;border-radius:2px;background:#2563EB;vertical-align:middle;margin-right:3px;"></span>
        Crosswind
      </span>
    </div>
    {% endif %}
    ```

    **E. Mobile responsive (in the @media block for .plan-table around line ~860):**
    No change needed — the inline styles handle sizing. The wind cell will naturally compress with the table.

    CRITICAL placement details:
    - Wind column goes AFTER "Avg Speed" and BEFORE "Time Bank" (conditional) / "Diff." in ALL three locations (thead, tbody, tfoot)
    - The legend goes INSIDE the table-view div (id="table-view") so it hides when cards view is active, OR right after the table-view div with the same guard
    - Colors use the exact hex values from wind_cell_style: #16A34A (green/tailwind), #DC2626 (red/headwind), #2563EB (blue/crosswind)
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -x -q</automated>
  </verify>
  <done>Wind column header, per-row wind cells, footer cell, and legend block all present in template; all guarded by {% if stop_wind %}; no column count mismatch in tfoot; test suite still green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of wind column and legend</name>
  <files>routes/riders.py, templates/ride_plan_detail.html</files>
  <action>
    Human verifies the wind column renders correctly in the browser.

    What was built: Wind column in base ride plan table view with color-coded headwind/tailwind/crosswind cells and wind legend.

    Steps to verify:
    1. Start the local Flask dev server: python3 app.py
    2. Navigate to a ride plan that has an RWGPS route linked (e.g., one with rwgps_url_team set)
    3. Switch to Table view (if not default)
    4. Verify:
       a. A Wind column appears between Avg Speed and Time Bank (or Diff. if no cutoff)
       b. Each stop row has a colored wind cell — green for tailwind, red for headwind, blue for crosswind
       c. Color opacity varies (light winds are more transparent, strong winds more opaque)
       d. Font size varies (small text for light winds, larger for strong)
       e. Wind legend appears below the table with three colored squares and labels
    5. Navigate to a ride plan WITHOUT an RWGPS route
    6. Verify: No Wind column appears, no errors, page renders normally
    7. Refresh the page with RWGPS route — verify the page loads fast (cached, no API delay)
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -x -q</automated>
  </verify>
  <done>User confirms wind column displays correctly with proper colors, opacity scaling, font scaling, and legend; graceful degradation confirmed for plans without RWGPS routes</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -x -q` — full suite passes
- Ride plan with RWGPS route shows Wind column in table view
- Ride plan without RWGPS route shows no Wind column, no error
- Wind colors match: green=#16A34A (tailwind), red=#DC2626 (headwind), blue=#2563EB (crosswind)
- Legend block visible below table when wind data present
</verification>

<success_criteria>
- Wind column renders in table view for plans with RWGPS routes
- Wind cells have correct color coding with opacity and font scaling
- No column when wind unavailable — page renders cleanly
- Wind legend explains color coding
- Cache prevents duplicate API calls
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-forecast-wind-in-base-ride-plan/03-02-SUMMARY.md`
</output>
