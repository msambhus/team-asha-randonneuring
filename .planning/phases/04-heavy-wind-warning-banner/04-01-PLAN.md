---
phase: 04-heavy-wind-warning-banner
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/weather.py
  - tests/test_weather.py
autonomous: true
requirements:
  - WARN-01
  - WARN-03
  - WARN-04

must_haves:
  truths:
    - "detect_heavy_wind() returns a warning dict when max wind > 30 km/h"
    - "detect_heavy_wind() returns a warning dict when avg headwind > 15 km/h"
    - "detect_heavy_wind() returns None when winds are below both thresholds"
    - "detect_heavy_wind() returns None for empty or None input"
    - "Warning dict includes description with avg headwind and max gust values"
    - "fetch_stop_wind() output includes headwind_kmh per stop"
  artifacts:
    - path: "services/weather.py"
      provides: "detect_heavy_wind() function"
      contains: "def detect_heavy_wind"
    - path: "tests/test_weather.py"
      provides: "TestDetectHeavyWind test class"
      contains: "class TestDetectHeavyWind"
  key_links:
    - from: "services/weather.py::detect_heavy_wind"
      to: "services/weather.py::HEAVY_WIND_MAX_KMH"
      via: "threshold comparison"
      pattern: "HEAVY_WIND_MAX_KMH|HEAVY_WIND_AVG_HEADWIND_KMH"
    - from: "services/weather.py::detect_heavy_wind"
      to: "services/weather.py::fetch_stop_wind"
      via: "consumes return value"
      pattern: "headwind_kmh"
---

<objective>
Add detect_heavy_wind() pure function and augment fetch_stop_wind() with headwind_kmh field.

Purpose: detect_heavy_wind() is the core logic for Phase 4 — it evaluates per-stop wind data against HEAVY_WIND_MAX_KMH (30) and HEAVY_WIND_AVG_HEADWIND_KMH (15) thresholds. fetch_stop_wind() must also return headwind_kmh per stop, which it currently computes internally but discards.
Output: Tested detect_heavy_wind() function and backward-compatible headwind_kmh addition to fetch_stop_wind().
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-heavy-wind-warning-banner/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From services/weather.py (lines 16-17):
```python
HEAVY_WIND_MAX_KMH = 30
HEAVY_WIND_AVG_HEADWIND_KMH = 15
```

From services/weather.py — current fetch_stop_wind() return shape (line 411-416):
```python
result.append({
    'wind_speed_kmh': round(float(wind_speed), 1),
    'wind_type': wind_type,       # 'headwind' | 'tailwind' | 'crosswind'
    'style': style,               # dict from wind_cell_style()
    'label': wind_label(hw),      # display label string
})
# NOTE: hw (headwind component) is computed at line 406 but NOT included in output.
# This plan adds 'headwind_kmh': round(float(hw), 1) to the dict.
```

From services/weather.py — function signature:
```python
def fetch_stop_wind(stops, track_points, plan_slug, start_time_str, cache=None):
    """Fetch wind forecast for each stop along a route."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add headwind_kmh to fetch_stop_wind output and test detect_heavy_wind</name>
  <files>services/weather.py, tests/test_weather.py</files>
  <behavior>
    - Test: detect_heavy_wind(None) returns None
    - Test: detect_heavy_wind([]) returns None
    - Test: detect_heavy_wind([None, None]) returns None (all stops None)
    - Test: detect_heavy_wind with max_wind=35, avg_headwind=10 returns warning (max_wind > 30 triggers)
    - Test: detect_heavy_wind with max_wind=20, avg_headwind=18 returns warning (avg_headwind > 15 triggers)
    - Test: detect_heavy_wind with max_wind=25, avg_headwind=10 returns None (both below thresholds)
    - Test: detect_heavy_wind with max_wind=30, avg_headwind=15 returns None (strict >, not >=)
    - Test: Warning dict has keys max_wind_kmh, avg_headwind_kmh, is_heavy=True, description (string)
    - Test: description contains avg headwind and gust values as readable text
    - Test: Mixed list with some None stops skips None entries correctly
  </behavior>
  <action>
RED phase:
1. In tests/test_weather.py, add class TestDetectHeavyWind with test methods for all behaviors listed above. Use local imports inside each test method (matches existing test convention in this file). Build mock stop_wind lists directly as dicts with 'wind_speed_kmh' and 'headwind_kmh' keys.

2. Run tests — they must fail (detect_heavy_wind does not exist yet).

GREEN phase:
3. In services/weather.py, add 'headwind_kmh': round(float(hw), 1) to the result.append() dict at line 411, right after 'wind_speed_kmh'. This is backward-compatible — existing callers only read keys they need.

4. Add detect_heavy_wind(stop_wind) function after fetch_stop_wind(). Implementation:
   - Return None if stop_wind is falsy
   - Filter out None entries: valid = [s for s in stop_wind if s is not None]
   - Return None if valid is empty
   - Compute max_wind = max(s['wind_speed_kmh'] for s in valid)
   - Compute avg_headwind = sum(s['headwind_kmh'] for s in valid) / len(valid)
   - If max_wind > HEAVY_WIND_MAX_KMH or avg_headwind > HEAVY_WIND_AVG_HEADWIND_KMH, return dict with max_wind_kmh, avg_headwind_kmh, is_heavy=True, description
   - Use strict > (not >=) — consistent with classify_wind convention (Phase 1 decision)
   - Description format: "Strong headwinds expected -- avg {avg_headwind} km/h headwind, gusts to {max_wind} km/h"
   - Return None if below both thresholds

5. Run tests — all must pass.

REFACTOR: No refactor expected — function is simple.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_weather.py::TestDetectHeavyWind -x -q</automated>
  </verify>
  <done>
    - TestDetectHeavyWind class has 8+ test methods covering all edge cases
    - detect_heavy_wind() exists in services/weather.py and passes all tests
    - fetch_stop_wind() returns headwind_kmh in each stop dict
    - Existing tests still pass: python3 -m pytest tests/test_weather.py -x -q
  </done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_weather.py -x -q
</verification>

<success_criteria>
- detect_heavy_wind() returns warning dict for winds exceeding either threshold
- detect_heavy_wind() returns None for winds below both thresholds
- detect_heavy_wind() handles None/empty input gracefully
- fetch_stop_wind() backward-compatible with headwind_kmh field added
- All existing weather tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-heavy-wind-warning-banner/04-01-SUMMARY.md`
</output>
