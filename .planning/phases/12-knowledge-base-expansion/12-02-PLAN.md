---
phase: 12-knowledge-base-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - models.py
  - routes/admin.py
  - templates/admin/knowledge.html
  - tests/test_knowledge_admin.py
autonomous: true
requirements:
  - KB-04
  - KB-05
  - KB-06

must_haves:
  truths:
    - "Admin can see a list of all web_ sources with URL, embed date, and chunk count"
    - "Admin can trigger re-embed for a specific source (deletes chunks, shows CLI instruction)"
    - "Admin can remove all embeddings from a specific source"
  artifacts:
    - path: "models.py"
      provides: "get_knowledge_sources() and delete_knowledge_source() functions"
      contains: "def get_knowledge_sources"
    - path: "routes/admin.py"
      provides: "Knowledge admin routes: list, re-embed, remove"
      contains: "def knowledge"
    - path: "templates/admin/knowledge.html"
      provides: "Admin knowledge base page with source table"
      contains: "Knowledge Base"
    - path: "tests/test_knowledge_admin.py"
      provides: "Unit tests for knowledge admin model + routes"
      min_lines: 40
  key_links:
    - from: "routes/admin.py"
      to: "models.py"
      via: "get_knowledge_sources(), delete_knowledge_source()"
      pattern: "from models import"
    - from: "templates/admin/knowledge.html"
      to: "routes/admin.py"
      via: "render_template with sources context"
      pattern: "sources"
---

<objective>
Create the admin knowledge base page at /admin/knowledge that lists all embedded web sources with chunk counts and dates, lets the admin remove a source's embeddings, and provides a re-embed trigger (delete + CLI instruction per Vercel serverless constraint).

Purpose: Give the admin visibility and control over the expanded knowledge base without code changes.
Output: Model functions, admin routes, Jinja template, unit tests.
</objective>

<execution_context>
@/Users/msambhus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/msambhus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-knowledge-base-expansion/12-RESEARCH.md
@.planning/phases/12-knowledge-base-expansion/12-01-SUMMARY.md

<interfaces>
<!-- Existing admin route pattern from routes/admin.py -->
```python
@admin_bp.route('/guardrails')
@user_login_required
def guardrails():
    _require_admin()
    from models import get_all_guardrails
    guardrails = get_all_guardrails()
    return render_template('admin/guardrails.html', guardrails=guardrails)
```

<!-- Existing admin template pattern from templates/admin/guardrails.html -->
```html
{% extends "base.html" %}
{% block title %}Coaching Guardrails - Admin{% endblock %}
{% block content %}
<div class="hero" style="padding:40px 20px;">
<h1>Coaching Guardrails</h1>
</div>
<div class="container section">
<div style="margin-bottom:20px;...">
<a href="{{ url_for('admin.dashboard') }}" class="btn" ...>Back to Dashboard</a>
</div>
<div class="table-wrap"><table>...</table></div>
</div>
{% endblock %}
```

<!-- Model helper pattern from models.py -->
```python
def _execute(sql, params=None):
    """Execute a query and return a RealDictCursor."""
    conn = get_db()
    ...
```

<!-- whatsapp_chunk table columns relevant to knowledge admin -->
```sql
-- source TEXT NOT NULL (web_* prefix for web chunks)
-- content_hash TEXT (added by migration 013)
-- created_at TIMESTAMPTZ DEFAULT NOW()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model functions + tests</name>
  <files>models.py, tests/test_knowledge_admin.py</files>
  <behavior>
    - test_get_knowledge_sources: mock _execute returns rows with source/chunk_count/first_embedded/last_embedded; function returns them filtered to web_ prefix
    - test_get_knowledge_sources_empty: mock _execute returns empty list; function returns empty list
    - test_delete_knowledge_source: mock _execute with DELETE RETURNING; function returns count of deleted rows
    - test_delete_knowledge_source_rejects_non_web: function raises ValueError for source not starting with web_
  </behavior>
  <action>
    1. Create tests/test_knowledge_admin.py with failing tests for model functions. Mock models._execute or models.get_db as appropriate (follow existing test patterns in the project).

    2. Add to models.py two new functions:

    get_knowledge_sources() -> list[dict]:
      Execute: SELECT source, COUNT(*) AS chunk_count, MIN(created_at) AS first_embedded, MAX(created_at) AS last_embedded FROM whatsapp_chunk WHERE source LIKE 'web_%' GROUP BY source ORDER BY last_embedded DESC
      Return list of RealDictRow results.

    delete_knowledge_source(source: str) -> int:
      Validate source starts with 'web_' — raise ValueError if not.
      Execute: DELETE FROM whatsapp_chunk WHERE source = %s RETURNING id
      Return len(fetchall()) as deleted count.
      Commit the transaction.
  </action>
  <verify>
    <automated>cd /Users/msambhus/LocalDocuments/Personal/TeamAshaRandonneuring && python3 -m pytest tests/test_knowledge_admin.py -x -q</automated>
  </verify>
  <done>Model functions exist in models.py. All 4 tests pass. delete_knowledge_source rejects non-web_ sources.</done>
</task>

<task type="auto">
  <name>Task 2: Admin routes + template + dashboard link</name>
  <files>routes/admin.py, templates/admin/knowledge.html, templates/admin/dashboard.html</files>
  <action>
    1. Add three routes to routes/admin.py:

    GET /admin/knowledge:
      - _require_admin()
      - from models import get_knowledge_sources
      - sources = get_knowledge_sources()
      - render_template('admin/knowledge.html', sources=sources)

    POST /admin/knowledge/<path:source>/remove:
      - _require_admin()
      - Validate source starts with 'web_' else flash error + redirect
      - from models import delete_knowledge_source
      - count = delete_knowledge_source(source)
      - flash(f'Removed {count} chunks from {source}.', 'success')
      - redirect to admin.knowledge

    POST /admin/knowledge/<path:source>/re-embed:
      - _require_admin()
      - Validate source starts with 'web_'
      - from models import delete_knowledge_source
      - count = delete_knowledge_source(source)
      - flash(f'Cleared {count} chunks from {source}. Run: python scripts/embed_resources.py --source {source}', 'info')
      - redirect to admin.knowledge

    2. Create templates/admin/knowledge.html following the guardrails.html pattern:
      - extends base.html, block title "Knowledge Base - Admin"
      - Hero section with "Knowledge Base" title and source count
      - Back to Dashboard button
      - Table with columns: Source, Chunks, First Embedded, Last Embedded, Actions
      - Each row shows source name, chunk_count, first_embedded|date, last_embedded|date
      - Actions column: two form buttons — "Re-embed" (POST to re-embed route) and "Remove" (POST to remove route with onclick="return confirm('Remove all chunks?')")
      - If no sources, show "No web sources embedded yet. Run scripts/embed_resources.py to get started." message
      - Use existing Tailwind/CSS variable styling consistent with other admin pages

    3. Add a "Knowledge Base" link to templates/admin/dashboard.html in the admin navigation section, linking to url_for('admin.knowledge'). Follow the pattern of existing links (Personalities, Gear, Coaches, Guardrails).
  </action>
  <verify>
    <automated>cd /Users/msambhus/LocalDocuments/Personal/TeamAshaRandonneuring && python3 -m pytest tests/test_knowledge_admin.py tests/test_embed_resources.py -x -q</automated>
  </verify>
  <done>Admin knowledge page exists at /admin/knowledge. Sources listed with chunk counts and dates. Re-embed clears chunks and shows CLI instruction. Remove deletes all chunks for a source. Dashboard has Knowledge Base link.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_knowledge_admin.py tests/test_embed_resources.py -x -q` — all tests green
- `python3 -m pytest tests/ -x -q` — full suite green (no regressions)
- Admin knowledge page renders with correct table structure
- Remove and re-embed routes function correctly with flash messages
</verification>

<success_criteria>
- /admin/knowledge shows all web_ sources with chunk count, first/last embed date
- Admin can remove a source (all its chunks deleted from whatsapp_chunk)
- Admin can re-embed a source (chunks deleted + CLI instruction shown)
- Dashboard links to knowledge page
- All tests pass, no regressions in full suite
</success_criteria>

<output>
After completion, create `.planning/phases/12-knowledge-base-expansion/12-02-SUMMARY.md`
</output>
